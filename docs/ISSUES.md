# 📋 DANH SÁCH 40 GITHUB ISSUES CHI TIẾT - DỰ ÁN TECHSHARE
> **Môn học**: MMA301 - Cross-Platform Mobile Applications Development  
> **Quy mô**: 5 Thành viên (**An, Cường, Hạo, Kiên, Nhật (Lead)**)  
> **Công nghệ**: React Native (Expo SDK 57), Node.js (Express), MongoDB Atlas, Google Gemini AI  
> **Tài liệu tham chiếu**: [FEATURES.md](file:///e:/Repo/tech-share/docs/FEATURES.md), [MEMBER_ASSIGNMENTS.md](file:///e:/Repo/tech-share/docs/MEMBER_ASSIGNMENTS.md), [API_SPECIFICATION.md](file:///e:/Repo/tech-share/docs/API_SPECIFICATION.md), [DATABASE_DESIGN.md](file:///e:/Repo/tech-share/docs/DATABASE_DESIGN.md)

---

## 📑 BẢNG TỔNG HỢP 40 ISSUES PHÂN CHIA CHO 5 THÀNH VIÊN

| STT | Issue | Tiêu đề | Phụ trách | Độ ưu tiên | Sprint |
| :---: | :---: | :--- | :--- | :---: | :---: |
| **THÀNH VIÊN 1: AN (MOBILE ARCHITECT & SECURITY ENGINEER)** |
| 1 | **[#1](#issue-1-feature-bo-khung-dieu-huong-3-tang-rootnavigator-appdrawer-bottomtabs)** | [Feature] Bộ khung Điều hướng 3 tầng: RootNavigator, AppDrawer, BottomTabs | An | 🔴 `priority::P0` | Sprint 0 |
| 2 | **[#2](#issue-2-feature-dang-ky-tai-khoan-register-formik-yup-bcryptjs)** | [Feature] Đăng ký tài khoản (Register) Formik + Yup & bcryptjs | An | 🔴 `priority::P0` | Sprint 1 |
| 3 | **[#3](#issue-3-feature-dang-nhap-login-jwt-token-asyncstorage-session)** | [Feature] Đăng nhập (Login), JWT Token & AsyncStorage Session | An | 🔴 `priority::P0` | Sprint 1 |
| 4 | **[#4](#issue-4-feature-ho-so-ca-nhan-profile-viewedit-avatar-dia-chi)** | [Feature] Hồ sơ cá nhân (Profile View/Edit), Avatar & Địa chỉ | An | 🔴 `priority::P0` | Sprint 1 |
| 5 | **[#5](#issue-5-feature-onboarding-slider-kham-pha-khi-mo-app-lan-dau)** | [Feature] Onboarding Slider khám phá khi mở app lần đầu | An | 🟡 `priority::P1` | Sprint 1 |
| 6 | **[#6](#issue-6-feature-xac-thuc-sinh-trac-hoc-biometrics-faceid-van-tay)** | [Feature] Xác thực Sinh trắc học (Biometrics: FaceID / Vân tay) | An | 🟢 `priority::P2` | Sprint 3 |
| 7 | **[#7](#issue-7-feature-xac-minh-danh-tinh-dien-tu-ekyc-quet-cccd-2-mat)** | [Feature] Xác minh danh tính điện tử (eKYC: Quét CCCD 2 mặt) | An | 🟡 `priority::P1` | Sprint 3 |
| 8 | **[#8](#issue-8-feature-he-thong-diem-tin-nhiem-cong-nghe-trust-score)** | [Feature] Hệ thống Điểm Tín nhiệm công nghệ (Trust Score) | An | 🟢 `priority::P2` | Sprint 3 |
| **THÀNH VIÊN 2: CƯỜNG (FRONTEND ENGINEER - DISCOVERY & MAPS)** |
| 9 | **[#9](#issue-9-feature-trang-chu-home-screen-thanh-cuon-6-danh-muc)** | [Feature] Trang chủ (Home Screen) & Thanh cuộn 6 danh mục | Cường | 🔴 `priority::P0` | Sprint 1 |
| 10 | **[#10](#issue-10-feature-luoi-thiet-bi-device-grid-the-san-pham-device-card)** | [Feature] Lưới thiết bị (Device Grid) & Thẻ sản phẩm (Device Card) | Cường | 🔴 `priority::P0` | Sprint 1 |
| 11 | **[#11](#issue-11-feature-chi-tiet-thiet-bi-device-detail-dynamic-specs-sheet)** | [Feature] Chi tiết thiết bị (Device Detail) & Dynamic Specs Sheet | Cường | 🔴 `priority::P0` | Sprint 1 |
| 12 | **[#12](#issue-12-feature-tim-kiem-tuc-thi-live-search-debounce-tu-khoa-hot)** | [Feature] Tìm kiếm tức thì (Live Search Debounce) & Từ khóa hot | Cường | 🔴 `priority::P0` | Sprint 1 |
| 13 | **[#13](#issue-13-feature-modal-bo-loc-nang-cao-da-tieu-chi-multi-filter)** | [Feature] Modal Bộ lọc nâng cao đa tiêu chí (Multi-Filter) | Cường | 🟡 `priority::P1` | Sprint 2 |
| 14 | **[#14](#issue-14-feature-ban-do-gps-tuong-tac-react-native-maps-expo-location)** | [Feature] Bản đồ GPS tương tác (react-native-maps + expo-location) | Cường | 🔴 `priority::P0` | Sprint 1 |
| 15 | **[#15](#issue-15-feature-custom-marker-the-callout-tom-tat-thong-minh)** | [Feature] Custom Marker & Thẻ Callout tóm tắt thông minh | Cường | 🟡 `priority::P1` | Sprint 2 |
| 16 | **[#16](#issue-16-feature-phan-cum-marker-ban-do-clustering-supercluster)** | [Feature] Phân cụm Marker bản đồ (Clustering SuperCluster) | Cường | 🟢 `priority::P2` | Sprint 3 |
| **THÀNH VIÊN 3: HẠO (FRONTEND ENGINEER - BOOKING & OFFLINE)** |
| 17 | **[#17](#issue-17-feature-man-hinh-dat-thue-booking-create-date-range-picker)** | [Feature] Màn hình Đặt thuê (Booking Create) & Date Range Picker | Hạo | 🔴 `priority::P0` | Sprint 2 |
| 18 | **[#18](#issue-18-feature-bo-tinh-chi-phi-tu-dong-chiet-khau-dai-ngay-voucher)** | [Feature] Bộ tính chi phí tự động, Chiết khấu dài ngày & Voucher | Hạo | 🔴 `priority::P0` | Sprint 2 |
| 19 | **[#19](#issue-19-feature-quan-ly-don-cua-toi-my-bookings-5-tab-tien-trinh)** | [Feature] Quản lý Đơn của tôi (My Bookings) 5 tab tiến trình | Hạo | 🔴 `priority::P0` | Sprint 2 |
| 20 | **[#20](#issue-20-feature-chi-tiet-don-thue-timeline-so-do-tien-do-huy-don)** | [Feature] Chi tiết đơn thuê, Timeline sơ đồ tiến độ & Hủy đơn | Hạo | 🔴 `priority::P0` | Sprint 2 |
| 21 | **[#21](#issue-21-feature-dong-ho-dem-nguoc-countdown-yeu-cau-gia-han-thue)** | [Feature] Đồng hồ đếm ngược (Countdown) & Yêu cầu gia hạn thuê | Hạo | 🟡 `priority::P1` | Sprint 3 |
| 22 | **[#22](#issue-22-feature-ma-qr-ban-giao-chup-anh-nhan-may-beforerental)** | [Feature] Mã QR bàn giao & Chụp ảnh nhận máy (beforeRental) | Hạo | 🟡 `priority::P1` | Sprint 2 |
| 23 | **[#23](#issue-23-feature-danh-sach-yeu-thich-wishlist-co-che-offline-first)** | [Feature] Danh sách Yêu thích (Wishlist) & Cơ chế Offline-First | Hạo | 🟡 `priority::P1` | Sprint 2 |
| 24 | **[#24](#issue-24-feature-tao-don-nhap-ngoai-tuyen-draft-booking-background-sync)** | [Feature] Tạo đơn nháp ngoại tuyến (Draft Booking) & Background Sync | Hạo | 🟢 `priority::P2` | Sprint 3 |
| **THÀNH VIÊN 4: KIÊN (FULLSTACK & AI ENGINEER)** |
| 25 | **[#25](#issue-25-feature-form-dang-tin-cho-thue-moi-post-device-specs-map)** | [Feature] Form Đăng tin cho thuê mới (Post Device & Specs Map) | Kiên | 🔴 `priority::P0` | Sprint 1 |
| 26 | **[#26](#issue-26-feature-chupchon-anh-expo-image-picker-tu-gan-gps-geojson)** | [Feature] Chụp/Chọn ảnh expo-image-picker & Tự gắn GPS GeoJSON | Kiên | 🔴 `priority::P0` | Sprint 1 |
| 27 | **[#27](#issue-27-feature-quan-ly-thiet-bi-cua-toi-my-devices-status-toggle)** | [Feature] Quản lý Thiết bị của tôi (My Devices) & Status Toggle | Kiên | 🔴 `priority::P0` | Sprint 1 |
| 28 | **[#28](#issue-28-feature-lich-ban-ca-nhan-chu-may-availability-calendar)** | [Feature] Lịch bận cá nhân chủ máy (Availability Calendar) | Kiên | 🟡 `priority::P1` | Sprint 2 |
| 29 | **[#29](#issue-29-feature-bao-cao-doanh-thu-thong-ke-ty-le-lap-day-utilization)** | [Feature] Báo cáo doanh thu & Thống kê tỷ lệ lấp đầy (Utilization) | Kiên | 🟡 `priority::P1` | Sprint 3 |
| 30 | **[#30](#issue-30-feature-gemini-ai-review-summarizer-the-pros-cons-bestfor)** | [Feature] Gemini AI: Review Summarizer thẻ Pros, Cons, BestFor | Kiên | 🔴 `priority::P0` | Sprint 2 |
| 31 | **[#31](#issue-31-feature-gemini-ai-device-comparator-so-sanh-2-thiet-bi)** | [Feature] Gemini AI: Device Comparator so sánh 2 thiết bị | Kiên | 🟡 `priority::P1` | Sprint 2 |
| 32 | **[#32](#issue-32-feature-gemini-ai-rental-consultant-ai-listing-assistant)** | [Feature] Gemini AI: Rental Consultant & AI Listing Assistant | Kiên | 🟢 `priority::P2` | Sprint 3 |
| **THÀNH VIÊN 5: NHẬT (LEAD - OPERATIONS & ADMIN)** |
| 33 | **[#33](#issue-33-feature-xu-ly-don-phia-chu-may-owner-approval-rejection)** | [Feature] Xử lý đơn phía Chủ máy (Owner Approval & Rejection) | Nhật (Lead) | 🔴 `priority::P0` | Sprint 2 |
| 34 | **[#34](#issue-34-feature-quet-qr-kich-hoat-active-anh-doi-soat-tra-afterrental)** | [Feature] Quét QR kích hoạt active & Ảnh đối soát trả (afterRental) | Nhật (Lead) | 🔴 `priority::P0` | Sprint 2 |
| 35 | **[#35](#issue-35-feature-khieu-nai-hu-hong-quy-trinh-tru-coc-damage-dispute)** | [Feature] Khiếu nại hư hỏng & Quy trình trừ cọc (Damage Dispute) | Nhật (Lead) | 🟡 `priority::P1` | Sprint 3 |
| 36 | **[#36](#issue-36-feature-he-thong-danh-gia-2-chieu-review-rating-1-5-sao-anh-that)** | [Feature] Hệ thống Đánh giá 2 chiều (Review & Rating 1-5 sao ảnh thật) | Nhật (Lead) | 🔴 `priority::P0` | Sprint 2 |
| 37 | **[#37](#issue-37-feature-nhan-tin-truc-tiep-1-1-in-app-chat-renter-owner)** | [Feature] Nhắn tin trực tiếp 1-1 In-App Chat Renter ⇋ Owner | Nhật (Lead) | 🟡 `priority::P1` | Sprint 3 |
| 38 | **[#38](#issue-38-feature-trung-tam-thong-bao-notification-hub-nhac-han-tra)** | [Feature] Trung tâm Thông báo (Notification Hub) & Nhắc hạn trả | Nhật (Lead) | 🔴 `priority::P0` | Sprint 2 |
| 39 | **[#39](#issue-39-feature-vi-ky-quy-noi-bo-mock-escrow-wallet-hoan-coc)** | [Feature] Ví ký quỹ nội bộ Mock (Escrow Wallet) & Hoàn cọc | Nhật (Lead) | 🟢 `priority::P2` | Sprint 3 |
| 40 | **[#40](#issue-40-feature-trung-tam-quan-tri-admin-hub-platform-governance)** | [Feature] Trung tâm Quản trị Admin (Admin Hub & Platform Governance) | Nhật (Lead) | 🟡 `priority::P1` | Sprint 3 |

---

## 🏛️ PHẦN 1: ISSUES CỦA THÀNH VIÊN 1 - AN
> **Vai trò**: Mobile Architect & Security Engineer  
> **Luồng phụ trách**: Khung điều hướng, Định danh, Xác thực, Hồ sơ cá nhân & eKYC Tín nhiệm

## ISSUE #1: [Feature] Bộ khung Điều hướng 3 tầng: RootNavigator, AppDrawer, BottomTabs
Assignee: An  
Labels: `type::feature` · `priority::P0` · `sprint::0`

### Bộ khung Điều hướng 3 tầng Description
Thiết lập kiến trúc điều hướng 3 tầng chuẩn React Navigation 7 trên Expo SDK 57: `RootNavigator` (Native Stack) điều phối màn hình xác thực và app chính; `AppDrawer` (Drawer Navigator) cho phép mở menu ngăn kéo theo role `both`; `BottomTabNavigator` (Bottom Tabs) điều hướng 4 tab chính (Khám phá, Bản đồ, Đơn thuê, Hồ sơ).

### Objective
Cung cấp bộ khung chuyển trang mượt mà, phân quyền giao diện linh hoạt giữa vai trò Người thuê và Chủ máy, không bị giật lag khi chuyển tab.

### Scope of Work
- [ ] Cấu hình `src/navigation/RootNavigator.tsx` với Native Stack.
- [ ] Cấu hình `src/navigation/AppDrawer.tsx` tích hợp Custom Drawer Header hiển thị avatar và menu role `both`.
- [ ] Cấu hình `src/navigation/BottomTabNavigator.tsx` gắn icon vector tương ứng cho 4 tab.
- [ ] Cấu hình TypeScript typings cho các tham số điều hướng (`NavigationParamList`).

### Acceptance Criteria
- [ ] Chuyển đổi giữa 4 tab đáy mượt mà, giữ nguyên trạng thái scroll của từng tab.
- [ ] Vuốt mép trái mở Drawer Menu hiển thị đúng phân mục "Đơn thuê của tôi" và "Thiết bị của tôi".
- [ ] Không phát sinh cảnh báo lỗi TypeScript trên các hook `useNavigation()` và `useRoute()`.

---

## ISSUE #2: [Feature] Đăng ký tài khoản (Register) Formik + Yup & bcryptjs
Assignee: An  
Labels: `type::feature` · `priority::P0` · `sprint::1`

### Đăng ký tài khoản (Register) Description
Xây dựng giao diện và luồng Đăng ký tài khoản mới: thu thập Họ tên, Email, Mật khẩu, Số điện thoại, Địa chỉ mặc định; kiểm tra lỗi dữ liệu đầu vào bằng Formik + Yup; mã hóa mật khẩu an toàn với `bcryptjs` phía backend và tự động gán role `both`.

### Objective
Đảm bảo người dùng mới dễ dàng tạo tài khoản an toàn, dữ liệu được xác thực chặt chẽ ở cả client và server, phòng chống đăng ký trùng lặp email.

### Scope of Work
- [ ] Xây dựng màn hình `src/screens/auth/RegisterScreen.tsx` tích hợp Formik và Yup schema.
- [ ] Kiểm tra tính hợp lệ: Email đúng cú pháp, Mật khẩu tối thiểu 6 ký tự gồm chữ và số, SĐT 10 chữ số.
- [ ] Xây dựng API Backend: `POST /api/auth/register` mã hóa mật khẩu với `bcryptjs.hash()`.
- [ ] Tự động gán `role: "both"` cho tài khoản mới đăng ký.

### Acceptance Criteria
- [ ] Đăng ký thành công trả về HTTP Status 201 kèm JWT Token và thông tin user.
- [ ] Nhập email đã tồn tại hiển thị thông báo lỗi: "Email này đã được sử dụng".
- [ ] Form hiển thị dòng chữ đỏ báo lỗi tức thì khi người dùng nhập sai quy chuẩn.

---

## ISSUE #3: [Feature] Đăng nhập (Login), JWT Token & AsyncStorage Session
Assignee: An  
Labels: `type::feature` · `priority::P0` · `sprint::1`

### Đăng nhập (Login), JWT Token & AsyncStorage Session Description
Xây dựng màn hình Đăng nhập bảo mật: nhập Email và Mật khẩu, so khớp mật khẩu mã hóa phía backend, cấp phát JWT Token (hạn 30 ngày), tự động lưu token và thông tin user vào AsyncStorage, tự khôi phục phiên đăng nhập khi mở app.

### Objective
Cung cấp cổng định danh tin cậy cho ứng dụng, duy trì trạng thái đăng nhập bền vững của người dùng qua các phiên mở app mà không bắt đăng nhập lại nhiều lần.

### Scope of Work
- [ ] Xây dựng màn hình `src/screens/auth/LoginScreen.tsx` với Formik + Yup.
- [ ] Xây dựng Redux slice `src/store/slices/authSlice.ts` quản lý `token`, `user`, `isAuthenticated`.
- [ ] Xây dựng API Backend: `POST /api/auth/login` kiểm tra mật khẩu bằng `bcryptjs.compare()` và ký token JWT.
- [ ] Viết hàm `loadStoredSession()` khởi tạo tự động trong `App.tsx` đọc token từ AsyncStorage.
- [ ] Cấu hình Axios Interceptor tự gắn header `Authorization: Bearer <token>` cho mọi request bảo vệ.

### Acceptance Criteria
- [ ] Đăng nhập đúng điều hướng thẳng vào màn hình Trang chủ (Home).
- [ ] Đăng nhập sai mật khẩu hiển thị thông báo: "Email hoặc mật khẩu không chính xác".
- [ ] Tắt hoàn toàn app và mở lại, ứng dụng tự động nhận diện đã đăng nhập và vào thẳng Home.

---

## ISSUE #4: [Feature] Hồ sơ cá nhân (Profile View/Edit), Avatar & Địa chỉ
Assignee: An  
Labels: `type::feature` · `priority::P0` · `sprint::1`

### Hồ sơ cá nhân (Profile View/Edit) Description
Xây dựng màn hình Hồ sơ người dùng (`ProfileScreen.tsx`): hiển thị thông tin cá nhân, ảnh đại diện, số điện thoại, địa chỉ nhận hàng mặc định, huy hiệu xác tín và nút Đăng xuất (Logout) xóa sạch session.

### Objective
Cho phép người dùng chủ động quản lý thông tin định danh của mình, chuẩn hóa địa chỉ nhận hàng phục vụ cho luồng giao nhận thiết bị cho thuê.

### Scope of Work
- [ ] Xây dựng giao diện `src/screens/profile/ProfileScreen.tsx` hiển thị avatar, tên, email, SĐT, địa chỉ.
- [ ] Xây dựng Modal cập nhật thông tin cá nhân và tích hợp đổi ảnh đại diện.
- [ ] Xây dựng API Backend: `GET /api/auth/me` và `PUT /api/auth/profile`.
- [ ] Thêm nút Đăng xuất xóa sạch dữ liệu AsyncStorage và điều hướng về màn hình Login.

### Acceptance Criteria
- [ ] Thông tin hồ sơ hiển thị chính xác từ cơ sở dữ liệu MongoDB Atlas.
- [ ] Cập nhật số điện thoại và địa chỉ phản ánh ngay lập tức trên màn hình sau khi lưu.
- [ ] Bấm Đăng xuất xóa sạch token trong máy và đưa Redux state về null an toàn.

---

## ISSUE #5: [Feature] Onboarding Slider khám phá khi mở app lần đầu
Assignee: An  
Labels: `type::feature` · `priority::P1` · `sprint::1`

### Onboarding Slider Description
Xây dựng màn hình Onboarding Carousel gồm 3 slide hình ảnh và nội dung nổi bật giới thiệu giá trị cốt lõi của TechShare: Thuê máy Flagship tiết kiệm ➔ Trợ lý Gemini AI review ➔ Định vị GPS quanh đây.

### Objective
Gây ấn tượng thị giác chuyên nghiệp cho người dùng trong lần đầu tiên mở ứng dụng, giải thích nhanh luồng hoạt động của nền tảng.

### Scope of Work
- [ ] Xây dựng component `src/screens/auth/OnboardingScreen.tsx` dạng ngang với pagination dots.
- [ ] Nút "Bỏ qua" (Skip) và nút "Bắt đầu ngay" (Get Started) chuyển sang màn hình Đăng nhập / Khám phá.
- [ ] Lưu cờ `hasSeenOnboarding: true` vào AsyncStorage để không hiển thị lại ở những lần mở app sau.

### Acceptance Criteria
- [ ] Lướt qua lại giữa 3 slide mượt mà với hiệu ứng chuyển trang sinh động.
- [ ] Nhấn "Bắt đầu ngay" điều hướng chính xác tới màn hình Trang chủ/Login.
- [ ] Lần mở app thứ 2 trở đi sẽ tự động bỏ qua Onboarding và vào thẳng app.

---

## ISSUE #6: [Feature] Xác thực Sinh trắc học (Biometrics: FaceID / Vân tay)
Assignee: An  
Labels: `type::feature` · `priority::P2` · `sprint::3`

### Xác thực Sinh trắc học Description
Tích hợp thư viện `expo-local-authentication` cho phép người dùng mở khóa ứng dụng nhanh chóng bằng Cảm biến vân tay (Fingerprint) hoặc Nhận diện khuôn mặt (FaceID) thay vì phải nhập lại mật khẩu.

### Objective
Nâng cao tính bảo mật và hiện đại hóa trải nghiệm người dùng trên thiết bị di động, rút ngắn thời gian xác thực tài khoản khi mở ứng dụng.

### Scope of Work
- [ ] Tích hợp `expo-local-authentication` kiểm tra phần cứng thiết bị có hỗ trợ sinh trắc học hay không (`hasHardwareAsync`).
- [ ] Tạo tùy chọn trong Profile: "Bật đăng nhập bằng Sinh trắc học".
- [ ] Kích hoạt popup xác thực sinh trắc học của hệ điều hành khi mở app hoặc khi xác nhận giao dịch cọc.

### Acceptance Criteria
- [ ] Thiết bị có hỗ trợ FaceID/Vân tay mở popup quét hợp lệ và xác thực thành công.
- [ ] Nếu quét thất bại quá 3 lần, ứng dụng tự động yêu cầu nhập mật khẩu dự phòng.

---

## ISSUE #7: [Feature] Xác minh danh tính điện tử (eKYC: Quét CCCD 2 mặt)
Assignee: An  
Labels: `type::feature` · `priority::P1` · `sprint::3`

### Xác minh danh tính điện tử (eKYC) Description
Xây dựng luồng xác minh danh tính người dùng: cho phép chụp và tải lên ảnh 2 mặt Căn cước công dân (CCCD) cùng ảnh chụp selfie chân dung; gửi yêu cầu đến Admin để xét duyệt cấp Huy hiệu Tích xanh Uy tín (`isVerified: true`).

### Objective
Xác minh danh tính thật của khách thuê máy, giảm thiểu rủi ro lừa đảo hoặc chiếm đoạt tài sản công nghệ có giá trị cao.

### Scope of Work
- [ ] Xây dựng màn hình `src/screens/profile/EkycVerificationScreen.tsx`: Khung chụp ảnh mặt trước CCCD, mặt sau CCCD và ảnh chân dung.
- [ ] Xây dựng API Backend: `POST /api/auth/ekyc` lưu trữ thông tin ảnh định danh vào tài khoản user.
- [ ] Hiển thị trạng thái hồ sơ eKYC: "Chờ duyệt", "Đã xác minh (Tích xanh)" hoặc "Bị từ chối".

### Acceptance Criteria
- [ ] Người dùng tải đủ 3 ảnh và nhấn gửi thành công, trạng thái chuyển sang "Đang chờ Admin duyệt".
- [ ] Sau khi được duyệt, huy hiệu Tích xanh hiển thị nổi bật cạnh tên người dùng trên toàn ứng dụng.

---

## ISSUE #8: [Feature] Hệ thống Điểm Tín nhiệm công nghệ (Trust Score)
Assignee: An  
Labels: `type::feature` · `priority::P2` · `sprint::3`

### Hệ thống Điểm Tín nhiệm Description
Xây dựng cơ chế chấm điểm tín nhiệm tài khoản (thang điểm 100): tự động cộng điểm khi hoàn tất đơn đúng hạn (+5) hoặc nhận đánh giá 5 sao (+3); trừ điểm khi trả máy trễ (-10) hoặc làm hỏng máy (-30). Điểm cao được hưởng chính sách giảm tiền cọc.

### Objective
Thúc đẩy ý thức giữ gìn thiết bị và tạo dựng uy tín cộng đồng lâu dài giữa người thuê và chủ máy.

### Scope of Work
- [ ] Thêm trường `trustScore: { type: Number, default: 100 }` vào Schema User.
- [ ] Xây dựng component `src/components/profile/TrustScoreCard.tsx` hiển thị điểm số và thanh tiến trình màu sắc.
- [ ] Xây dựng API Backend `GET /api/users/:id/trust-score` và hàm tự động tính toán điểm khi đơn hoàn tất.

### Acceptance Criteria
- [ ] Màn hình Profile hiển thị rõ điểm tín nhiệm hiện tại và lịch sử biến động điểm (+/ - điểm).
- [ ] Người dùng có điểm tín nhiệm trên 120 điểm tự động được giảm 20% tiền cọc khi tạo đơn thuê mới.

---

## 🔍 PHẦN 2: ISSUES CỦA THÀNH VIÊN 2 - CƯỜNG
> **Vai trò**: Frontend Engineer (Discovery & Maps)  
> **Luồng phụ trách**: Khám phá Thiết bị, Tìm kiếm đa tiêu chí & Bản đồ không gian GPS

## ISSUE #9: [Feature] Trang chủ (Home Screen) & Thanh cuộn 6 danh mục
Assignee: Cường  
Labels: `type::feature` · `priority::P0` · `sprint::1`

### Trang chủ (Home Screen) Description
Xây dựng giao diện Trang chủ (`HomeScreen.tsx`) gồm thanh tìm kiếm nhanh, carousel banner khuyến mãi, thanh trượt ngang chọn 6 danh mục công nghệ chuẩn (`Smartphone`, `Laptop`, `Camera`, `Drone`, `Audio`, `Phụ kiện`) và danh sách thiết bị sẵn sàng cho thuê.

### Objective
Tạo giao diện cửa ngõ ấn tượng, giúp người dùng dễ dàng định hướng và nhanh chóng truy cập vào danh mục thiết bị công nghệ mình đang tìm kiếm.

### Scope of Work
- [ ] Xây dựng `src/screens/home/HomeScreen.tsx` với cấu trúc cuộn mượt mà.
- [ ] Xây dựng component `src/components/home/CategoryBar.tsx` hiển thị icon và nhãn của 6 danh mục.
- [ ] Xây dựng banner quảng bá trượt tự động (Auto-scroll Carousel).
- [ ] Kết nối Redux action `fetchDevices` gọi API `GET /api/devices`.

### Acceptance Criteria
- [ ] Chạm vào từng icon danh mục sẽ lọc tức thì danh sách máy bên dưới theo đúng danh mục được chọn.
- [ ] Tích hợp tính năng vuốt kéo từ trên xuống (Pull-to-refresh) để làm mới danh sách thiết bị.

---

## ISSUE #10: [Feature] Lưới thiết bị (Device Grid) & Thẻ sản phẩm (Device Card)
Assignee: Cường  
Labels: `type::feature` · `priority::P0` · `sprint::1`

### Lưới thiết bị (Device Grid) Description
Xây dựng component hiển thị danh sách thiết bị dạng lưới 2 cột (`DeviceGrid.tsx`) và thẻ card thiết bị (`DeviceCard.tsx`) hiển thị đầy đủ: ảnh đại diện, nhãn hãng sản xuất, tên máy, giá thuê/ngày, rating sao trung bình, khoảng cách km và nút thả tim.

### Objective
Tối ưu hóa khả năng hiển thị hàng loạt thiết bị với giao diện hiện đại, cân đối và tải nhanh trên nhiều kích cỡ màn hình Android/iOS.

### Scope of Work
- [ ] Xây dựng component `src/components/device/DeviceCard.tsx`.
- [ ] Xây dựng component `src/components/device/DeviceGrid.tsx` sử dụng FlatList ảo hóa.
- [ ] Hiển thị hiệu ứng Skeleton Placeholder khi dữ liệu đang tải từ server.
- [ ] Chạm vào card chuyển hướng tới màn hình Chi tiết thiết bị (`DeviceDetailScreen`).

### Acceptance Criteria
- [ ] Danh sách hiển thị đều đặn 2 cột, không bị méo ảnh hoặc tràn chữ trên màn hình nhỏ.
- [ ] Lướt danh sách cuộn đạt tốc độ 60 FPS, ảnh thumbnail được tải đệm trơn tru.

---

## ISSUE #11: [Feature] Chi tiết thiết bị (Device Detail) & Dynamic Specs Sheet
Assignee: Cường  
Labels: `type::feature` · `priority::P0` · `sprint::1`

### Chi tiết thiết bị Description
Xây dựng màn hình Chi tiết thiết bị (`DeviceDetailScreen.tsx`): Carousel lướt xem album ảnh thực tế có bộ đếm trang, bảng thông số kỹ thuật động (Specs Map), tiền cọc bảo đảm, mô tả tình trạng máy, hồ sơ chủ máy và thanh dính ở đáy (Sticky Bottom Bar) có nút "Đặt thuê ngay".

### Objective
Cung cấp toàn diện và minh bạch mọi thông tin về cấu hình, hiện trạng thực tế và chi phí của thiết bị để khách thuê đưa ra quyết định chính xác.

### Scope of Work
- [ ] Xây dựng `src/screens/device/DeviceDetailScreen.tsx`.
- [ ] Xây dựng Carousel album ảnh hỗ trợ phóng to toàn màn hình (Zoom modal).
- [ ] Xây dựng component `src/components/device/SpecsTable.tsx` tự động render các cặp Key-Value thông số.
- [ ] Xây dựng Card thông tin Chủ máy kèm rating sao và nút liên hệ.
- [ ] Gọi API `GET /api/devices/:id` kèm Mongoose populate thông tin chủ sở hữu.

### Acceptance Criteria
- [ ] Bảng thông số kỹ thuật hiển thị ngay ngắn, tự động ẩn các trường bị trống.
- [ ] Nút "Đặt thuê ngay" chuyển mượt mà sang màn hình tạo đơn và truyền theo đúng `deviceId`.

---

## ISSUE #12: [Feature] Tìm kiếm tức thì (Live Search Debounce) & Từ khóa hot
Assignee: Cường  
Labels: `type::feature` · `priority::P0` · `sprint::1`

### Tìm kiếm tức thì Description
Xây dựng thanh tìm kiếm từ khóa thời gian thực trên Trang chủ: tích hợp cơ chế Debounce 400ms để giảm tải số lần gọi API, lưu lịch sử tìm kiếm gần nhất và hiển thị các gợi ý từ khóa xu hướng ("Sony FX3", "MacBook M3", "DJI Mini 4").

### Objective
Tối ưu hóa hành trình tra cứu thiết bị, giúp người dùng tìm ra sản phẩm mong muốn chỉ với vài ký tự gõ phím.

### Scope of Work
- [ ] Xây dựng component `src/components/home/SearchBar.tsx`.
- [ ] Viết custom hook `useDebounce` trì hoãn 400ms trước khi gửi request tìm kiếm.
- [ ] Cập nhật API Backend `GET /api/devices?keyword=...` hỗ trợ tìm kiếm Regex không phân biệt hoa thường.
- [ ] Lưu trữ và hiển thị 5 từ khóa tìm kiếm gần nhất vào AsyncStorage.

### Acceptance Criteria
- [ ] Nhập từ khóa hiển thị danh sách kết quả phù hợp sau đúng 400ms người dùng ngừng gõ.
- [ ] Bấm vào từ khóa trong lịch sử tìm kiếm sẽ tự động điền vào ô tìm kiếm và lọc danh sách.

---

## ISSUE #13: [Feature] Modal Bộ lọc nâng cao đa tiêu chí (Multi-Filter)
Assignee: Cường  
Labels: `type::feature` · `priority::P1` · `sprint::2`

### Modal Bộ lọc nâng cao Description
Xây dựng Modal bộ lọc đa tiêu chí (`FilterModal.tsx`): Lọc theo tầm giá thuê/ngày (thanh kéo Slider từ 50k đến 5 triệu), chọn hãng sản xuất (Apple, Sony, Canon, DJI...), chọn bán kính GPS (2km, 5km, 10km) và sắp xếp kết quả (giá tăng/giảm, sao cao nhất).

### Objective
Cho phép người thuê thu hẹp phạm vi tìm kiếm theo đúng ngân sách tài chính và sở thích thương hiệu cá nhân.

### Scope of Work
- [ ] Xây dựng component `src/components/home/FilterModal.tsx`.
- [ ] Tích hợp Slider chọn khoảng giá min - max.
- [ ] Cập nhật API Backend `GET /api/devices` tiếp nhận các tham số `minPrice`, `maxPrice`, `brand`, `sortBy`.
- [ ] Thêm nút "Đặt lại bộ lọc" đưa toàn bộ tiêu chí lọc về mặc định ban đầu.

### Acceptance Criteria
- [ ] Áp dụng bộ lọc giá từ 200k - 500k/ngày trả về chính xác các thiết bị nằm trong tầm giá đó.
- [ ] Nếu không có kết quả phù hợp, hiển thị thông báo "Không tìm thấy thiết bị" kèm nút reset bộ lọc.

---

## ISSUE #14: [Feature] Bản đồ GPS tương tác (react-native-maps + expo-location)
Assignee: Cường  
Labels: `type::feature` · `priority::P0` · `sprint::1`

### Bản đồ GPS tương tác Description
Xây dựng màn hình Bản đồ không gian (`MapScreen.tsx`) sử dụng thư viện `react-native-maps`: xin quyền truy cập vị trí qua `expo-location`, tự động căn giữa vị trí người dùng (chấm xanh) và hiển thị các ghim thiết bị cho thuê quanh bán kính GPS thực tế.

### Objective
Hiện thực hóa tính năng định vị không gian độc đáo, giúp khách thuê dễ dàng tìm và thuê máy gần vị trí của mình nhất để tiết kiệm thời gian di chuyển.

### Scope of Work
- [ ] Cấu hình quyền truy cập vị trí `ACCESS_FINE_LOCATION` trong `client/app.json`.
- [ ] Xây dựng màn hình `src/screens/map/MapScreen.tsx`.
- [ ] Sử dụng `expo-location.getCurrentPositionAsync` lấy tọa độ kinh độ và vĩ độ hiện tại.
- [ ] Xây dựng API Backend `GET /api/devices/nearby` sử dụng toán tử MongoDB `$nearSphere` và chỉ mục `2dsphere`.

### Acceptance Criteria
- [ ] Ứng dụng xin quyền vị trí thành công và hiển thị chấm xanh vị trí người dùng trên bản đồ.
- [ ] Bản đồ tự động tải và hiển thị các ghim thiết bị có sẵn trong bán kính lân cận.

---

## ISSUE #15: [Feature] Custom Marker & Thẻ Callout tóm tắt thông minh
Assignee: Cường  
Labels: `type::feature` · `priority::P1` · `sprint::2`

### Custom Marker & Thẻ Callout Description
Thiết kế các biểu tượng ghim bản đồ tùy biến (Custom Markers) phân biệt theo danh mục thiết bị (icon máy ảnh, laptop, drone...); khi người dùng nhấn vào ghim sẽ bung thẻ tóm tắt (Callout card) hiển thị ảnh thumbnail, giá thuê/ngày, khoảng cách km và nút bấm chuyển trang.

### Objective
Tăng tính trực quan và tương tác khi người dùng khám phá thiết bị trực tiếp trên bản đồ mà không cần phải rời khỏi màn hình bản đồ.

### Scope of Work
- [ ] Xây dựng component `src/components/map/DeviceMarker.tsx` gắn icon danh mục.
- [ ] Xây dựng component `src/components/map/MapCallout.tsx` hiển thị ảnh nhỏ, tên máy, giá thuê và khoảng cách km.
- [ ] Bắt sự kiện nhấn vào Callout card điều hướng thẳng sang màn hình `DeviceDetailScreen`.

### Acceptance Criteria
- [ ] Chạm vào từng ghim bản đồ mở ra thẻ Callout tóm tắt ngay trên đầu ghim.
- [ ] Chạm vào thẻ Callout chuyển mượt mà sang trang chi tiết của đúng thiết bị đó.

---

## ISSUE #16: [Feature] Phân cụm Marker bản đồ (Clustering SuperCluster)
Assignee: Cường  
Labels: `type::feature` · `priority::P2` · `sprint::3`

### Phân cụm Marker bản đồ Description
Tích hợp thuật toán gom cụm ghim bản đồ (Clustering): tự động gom các thiết bị nằm gần nhau thành một vòng tròn hiển thị số lượng khi người dùng thu nhỏ bản đồ (Zoom out), tự động tách ra thành từng ghim đơn lẻ khi phóng to bản đồ (Zoom in).

### Objective
Ngăn chặn hiện tượng chồng lấn hàng trăm ghim gây rối mắt và làm sụt giảm khung hình khi có mật độ thiết bị cao tại các thành phố lớn.

### Scope of Work
- [ ] Tích hợp giải pháp phân cụm bản đồ (`react-native-map-clustering` hoặc thư viện `supercluster`).
- [ ] Tùy biến giao diện vòng tròn cụm: màu sắc chuyển từ xanh lá sang cam/đỏ tùy theo số lượng máy trong cụm.
- [ ] Chạm vào vòng tròn cụm sẽ tự động phóng to bản đồ (Animate Zoom) vào tâm của cụm đó.

### Acceptance Criteria
- [ ] Bản đồ duy trì tốc độ khung hình mượt mà không bị giật lag khi hiển thị hơn 50 ghim thiết bị.
- [ ] Thu nhỏ và phóng to bản đồ thì các ghim gom cụm và tách rời tức thì, chính xác.

---

## 📦 PHẦN 3: ISSUES CỦA THÀNH VIÊN 3 - HẠO
> **Vai trò**: Frontend Engineer (Booking & Offline)  
> **Luồng phụ trách**: Quy trình Đặt thuê, Vòng đời Đơn hàng & Trải nghiệm Ngoại tuyến

## ISSUE #17: [Feature] Màn hình Đặt thuê (Booking Create) & Date Range Picker
Assignee: Hạo  
Labels: `type::feature` · `priority::P0` · `sprint::2`

### Màn hình Đặt thuê Description
Xây dựng màn hình Đặt thuê (`BookingCreateScreen.tsx`): tích hợp bộ lịch chọn khoảng ngày nhận và ngày trả máy (Date Range Picker); tự động làm mờ và chặn các ngày thiết bị đã có đơn thuê trùng lịch; nhập thông tin người nhận hàng với Formik + Yup.

### Objective
Tạo luồng đặt thuê rõ ràng, thu thập thông tin nhận máy chính xác và bảo đảm thiết bị không bị hai khách thuê đặt trùng một khoảng thời gian.

### Scope of Work
- [ ] Xây dựng màn hình `src/screens/booking/BookingCreateScreen.tsx`.
- [ ] Tích hợp component Calendar Date Range Picker hỗ trợ chọn ngày bắt đầu và kết thúc.
- [ ] Form nhập thông tin nhận hàng: Tên, SĐT, Địa chỉ nhận máy (validate bằng Formik + Yup).
- [ ] Xây dựng API Backend `POST /api/bookings` kiểm tra chặt chẽ chống trùng lịch thuê thiết bị.

### Acceptance Criteria
- [ ] Không cho phép chọn ngày nhận trong quá khứ hoặc ngày trả trước ngày nhận.
- [ ] Nếu khoảng ngày chọn bị trùng với đơn đã duyệt khác, hệ thống báo lỗi không cho tiếp tục.
- [ ] Bấm xác nhận tạo đơn lưu dữ liệu thành công với trạng thái mặc định `pending`.

---

## ISSUE #18: [Feature] Bộ tính chi phí tự động, Chiết khấu dài ngày & Voucher
Assignee: Hạo  
Labels: `type::feature` · `priority::P0` · `sprint::2`

### Bộ tính chi phí tự động Description
Xây dựng module tính toán tài chính tự động ngay trên màn hình tạo đơn: tính số ngày thuê thực tế, tiền thuê = `đơn giá/ngày * số ngày`, chiết khấu thuê dài ngày (>=3 ngày giảm 10%, >=7 ngày giảm 20%), tiền cọc bảo đảm và ô nhập áp dụng mã giảm giá (Voucher).

### Objective
Minh bạch hóa toàn bộ chi phí thuê máy, khuyến khích khách thuê dài ngày bằng chiết khấu tự động và hỗ trợ các chiến dịch khuyến mãi bằng voucher.

### Scope of Work
- [ ] Xây dựng component `src/components/booking/PriceBreakdownCard.tsx`.
- [ ] Viết hàm tính toán tự động cập nhật số tiền tức thì khi người dùng thay đổi ngày trên lịch.
- [ ] Xây dựng ô nhập mã Voucher kèm nút "Áp dụng" gọi API kiểm tra mã hợp lệ.
- [ ] Tổng thanh toán cuối cùng = `Tiền thuê - Chiết khấu - Giảm giá Voucher + Tiền cọc`.

### Acceptance Criteria
- [ ] Số tiền hiển thị tự động nhảy số chính xác ngay khi đổi ngày trên lịch.
- [ ] Nhập mã voucher hợp lệ (ví dụ: `WELCOME50`) lập tức trừ số tiền tương ứng vào tổng bill.
- [ ] Bảng kê chi phí hiển thị rõ ràng từng khoản mục giúp khách thuê an tâm.

---

## ISSUE #19: [Feature] Quản lý Đơn của tôi (My Bookings) 5 tab tiến trình
Assignee: Hạo  
Labels: `type::feature` · `priority::P0` · `sprint::2`

### Quản lý Đơn của tôi Description
Xây dựng màn hình Quản lý Đơn thuê của tôi (`MyBookingsScreen.tsx`) chia 5 tab tiến trình: Chờ duyệt (`pending`), Đã duyệt (`approved`), Đang thuê (`active`), Đã hoàn tất (`completed`), Đã hủy (`cancelled`); hiển thị danh sách thẻ đơn hàng kèm trạng thái màu sắc trực quan.

### Objective
Giúp người thuê dễ dàng theo dõi toàn bộ các đơn máy mình đang đặt thuê, nắm bắt tức thì tình trạng duyệt đơn từ phía chủ máy.

### Scope of Work
- [ ] Xây dựng `src/screens/booking/MyBookingsScreen.tsx` có thanh Top Tab Bar 5 trạng thái.
- [ ] Xây dựng component `src/components/booking/BookingItemCard.tsx`: mã đơn, ảnh máy, ngày thuê, tổng tiền, badge trạng thái.
- [ ] Xây dựng API Backend `GET /api/bookings/my-bookings` lọc theo query `status`.
- [ ] Hỗ trợ vuốt kéo làm mới danh sách đơn (Pull-to-refresh).

### Acceptance Criteria
- [ ] Danh sách đơn tải nhanh chóng, phân loại chính xác theo từng tab trạng thái.
- [ ] Chạm vào bất kỳ thẻ đơn hàng nào sẽ mở màn hình Chi tiết đơn thuê tương ứng.

---

## ISSUE #20: [Feature] Chi tiết đơn thuê, Timeline sơ đồ tiến độ & Hủy đơn
Assignee: Hạo  
Labels: `type::feature` · `priority::P0` · `sprint::2`

### Chi tiết đơn thuê & Hủy đơn Description
Xây dựng màn hình Chi tiết đơn thuê (`BookingDetailScreen.tsx`): hiển thị sơ đồ Timeline tiến độ đơn hàng (Đặt đơn ➔ Đã duyệt ➔ Đang thuê ➔ Hoàn tất), thông tin chủ máy, địa chỉ giao nhận và nút bấm "Hủy đơn thuê" khi đơn còn ở trạng thái `pending`.

### Objective
Cung cấp cái nhìn toàn diện về tiến trình thực hiện đơn hàng và cho phép khách thuê chủ động hủy đơn khi thay đổi kế hoạch mà không gây phiền hà.

### Scope of Work
- [ ] Xây dựng màn hình `src/screens/booking/BookingDetailScreen.tsx`.
- [ ] Xây dựng component `src/components/booking/OrderTimeline.tsx` với các icon tiến trình.
- [ ] Thêm nút bấm "Hủy đơn thuê" hiển thị popup hộp thoại xác nhận hủy đơn.
- [ ] Xây dựng API Backend `PATCH /api/bookings/:id/cancel` chuyển trạng thái đơn sang `cancelled`.

### Acceptance Criteria
- [ ] Sơ đồ Timeline hiển thị chính xác mốc thời gian của từng bước trong vòng đời đơn.
- [ ] Hủy đơn thành công khi đơn đang `pending`, đơn lập tức chuyển sang tab Đã hủy và mở lại lịch trống cho máy.
- [ ] Ẩn nút hủy đơn đối với các đơn đã được duyệt hoặc đang trong quá trình thuê.

---

## ISSUE #21: [Feature] Đồng hồ đếm ngược (Countdown) & Yêu cầu gia hạn thuê
Assignee: Hạo  
Labels: `type::feature` · `priority::P1` · `sprint::3`

### Đồng hồ đếm ngược & Gia hạn thuê Description
Xây dựng tiện ích Đồng hồ đếm ngược thời gian thuê (`RentalCountdownTimer.tsx`) hiển thị số ngày, giờ, phút còn lại trước hạn trả máy; xây dựng Modal "Gửi yêu cầu gia hạn thuê" cho phép người thuê đề xuất thuê thêm ngày trực tiếp tới chủ máy.

### Objective
Nhắc nhở người thuê sắp đến hạn trả máy tránh bị phạt trễ hạn, đồng thời tạo cơ hội tăng doanh thu cho chủ máy thông qua tính năng gia hạn trực tuyến.

### Scope of Work
- [ ] Xây dựng component `src/components/booking/RentalCountdownTimer.tsx` cập nhật thời gian theo từng giây.
- [ ] Xây dựng Modal "Yêu cầu gia hạn": chọn số ngày muốn thuê thêm và xem trước phụ phí phát sinh.
- [ ] Xây dựng API Backend `POST /api/bookings/:id/extend` gửi thông báo đề xuất gia hạn tới chủ máy.

### Acceptance Criteria
- [ ] Đồng hồ đếm ngược hiển thị chuẩn xác thời gian còn lại của gói thuê đối với các đơn `active`.
- [ ] Gửi yêu cầu gia hạn thành công, hệ thống thông báo gửi tới chủ máy xem xét duyệt.

---

## ISSUE #22: [Feature] Mã QR bàn giao & Chụp ảnh nhận máy (beforeRental)
Assignee: Hạo  
Labels: `type::feature` · `priority::P1` · `sprint::2`

### Mã QR bàn giao & Ảnh nhận máy Description
Xây dựng tính năng Giao nhận điện tử: màn hình Chi tiết đơn sinh mã QR đơn hàng để chủ máy quét xác nhận gặp mặt bàn giao; tích hợp Camera chụp ảnh 4 góc thiết bị và phụ kiện bàn giao lúc nhận máy (`handoverPhotos.beforeRental`).

### Objective
Lưu trữ bằng chứng hình ảnh hiện trạng ban đầu của thiết bị khi vừa nhận máy, bảo vệ người thuê khỏi các tranh chấp hỏng hóc vô cớ khi hoàn trả máy.

### Scope of Work
- [ ] Tích hợp thư viện sinh mã QR (`react-native-qrcode-svg`) hiển thị mã đơn trên màn hình chi tiết đơn.
- [ ] Xây dựng màn hình chụp ảnh nhận máy hỗ trợ chụp tối đa 4 góc máy và phụ kiện.
- [ ] Xây dựng API Backend lưu mảng URL ảnh vào trường `handoverPhotos.beforeRental` trong Collection `bookings`.

### Acceptance Criteria
- [ ] Mã QR hiển thị rõ nét, quét được tức thì bằng camera điện thoại của chủ máy.
- [ ] Ảnh chụp 4 góc máy được tải lên và lưu trữ an toàn trong cơ sở dữ liệu.

---

## ISSUE #23: [Feature] Danh sách Yêu thích (Wishlist) & Cơ chế Offline-First
Assignee: Hạo  
Labels: `type::feature` · `priority::P1` · `sprint::2`

### Danh sách Yêu thích & Cơ chế Offline-First Description
Xây dựng màn hình Danh sách Yêu thích (`WishlistScreen.tsx`) lưu các máy người dùng đã thả tim; thiết lập cơ chế Offline-First: tự động lưu bản sao danh sách thiết bị và đơn thuê vào AsyncStorage, tự động phát hiện mất mạng và đọc dữ liệu từ cache cục bộ mà không báo lỗi màn hình trắng.

### Objective
Đảm bảo trải nghiệm người dùng luôn liên tục và ổn định ngay cả khi đang ở khu vực sóng yếu hoặc mất kết nối Internet hoàn toàn.

### Scope of Work
- [ ] Xây dựng màn hình `src/screens/device/WishlistScreen.tsx`.
- [ ] Tích hợp NetInfo lắng nghe trạng thái kết nối mạng của thiết bị.
- [ ] Khi có mạng: tự động ghi đè bản cache mới nhất vào AsyncStorage.
- [ ] Khi mất mạng: đọc từ cache AsyncStorage và hiển thị thanh Banner nhẹ: "Đang xem dữ liệu ngoại tuyến".

### Acceptance Criteria
- [ ] Bấm nút thả tim trên bất kỳ máy nào, máy đó lập tức xuất hiện trong mục Wishlist.
- [ ] Bật chế độ máy bay (ngắt kết nối mạng), mở ứng dụng vẫn xem được danh sách Wishlist và lịch sử đơn mượt mà.

---

## ISSUE #24: [Feature] Tạo đơn nháp ngoại tuyến (Draft Booking) & Background Sync
Assignee: Hạo  
Labels: `type::feature` · `priority::P2` · `sprint::3`

### Tạo đơn nháp ngoại tuyến Description
Cho phép người dùng tạo Đơn đặt thuê nháp (Draft Booking) ngay cả khi điện thoại không có kết nối mạng; lưu đơn vào hàng đợi (Queue) cục bộ của AsyncStorage; khi thiết bị có mạng trở lại, hệ thống tự động đồng bộ đơn lên máy chủ ngầm.

### Objective
Giúp khách thuê không bị gián đoạn công việc đặt máy khi di chuyển qua các vùng mất sóng hoặc kết nối chập chờn.

### Scope of Work
- [ ] Xây dựng tiện ích hàng đợi ngoại tuyến `src/services/draftBookingQueue.ts`.
- [ ] Lưu đơn nháp vào AsyncStorage khi gọi API tạo đơn thất bại do mất kết nối mạng.
- [ ] Lắng nghe sự kiện mạng phục hồi để tự động gửi request `POST /api/bookings` lên server và xóa khỏi queue.

### Acceptance Criteria
- [ ] Tạo đơn khi không có mạng thông báo: "Đã lưu đơn nháp ngoại tuyến, đơn sẽ tự động gửi khi có mạng".
- [ ] Bật lại Wi-Fi/4G, đơn hàng tự động được đẩy lên server và xuất hiện trong tab Chờ duyệt.

---

## 🤖 PHẦN 4: ISSUES CỦA THÀNH VIÊN 4 - KIÊN
> **Vai trò**: Fullstack & AI Engineer  
> **Luồng phụ trách**: Đăng tin Cho thuê, Quản lý Kho máy & Trợ lý Gemini AI Hub

## ISSUE #25: [Feature] Form Đăng tin cho thuê mới (Post Device & Specs Map)
Assignee: Kiên  
Labels: `type::feature` · `priority::P0` · `sprint::1`

### Form Đăng tin cho thuê mới Description
Xây dựng màn hình Đăng tin cho thuê (`PostDeviceScreen.tsx`): form nhập đầy đủ thông tin máy (Tên máy, Thương hiệu, Danh mục, Năm sản xuất, Tình trạng máy, Giá thuê theo ngày, Giá trị cọc đề xuất, Mô tả chi tiết) và thêm các cặp thông số kỹ thuật động (Specs Map).

### Objective
Trang bị công cụ đăng tin tiện lợi cho chủ sở hữu thiết bị nhàn rỗi, cấu trúc hóa dữ liệu thông số để hiển thị đẹp mắt cho khách thuê.

### Scope of Work
- [ ] Xây dựng màn hình `src/screens/device/PostDeviceScreen.tsx`.
- [ ] Xây dựng component `src/components/device/SpecsInputForm.tsx` cho phép thêm/xóa linh hoạt các cặp key-value (RAM, Chip, Ổ cứng...).
- [ ] Xây dựng API Backend `POST /api/devices` lưu trữ thiết bị mới gán `owner: req.user._id`.

### Acceptance Criteria
- [ ] Form kiểm tra hợp lệ đầy đủ các trường bắt buộc (không được bỏ trống tên máy, giá thuê, tiền cọc).
- [ ] Đăng tin thành công trả về thông báo chúc mừng và hiển thị ngay thiết bị trên Trang chủ.

---

## ISSUE #26: [Feature] Chụp/Chọn ảnh expo-image-picker & Tự gắn GPS GeoJSON
Assignee: Kiên  
Labels: `type::feature` · `priority::P0` · `sprint::1`

### Chụp/Chọn ảnh & Tự gắn GPS Description
Tích hợp `expo-image-picker` cho phép chủ máy chụp ảnh thực tế từ Camera hoặc chọn tối đa 8 ảnh từ Thư viện; tích hợp `expo-location` tự động lấy tọa độ GPS vị trí hiện tại của thiết bị cất máy điền vào form dạng MongoDB GeoJSON `Point [kinh độ, vĩ độ]`.

### Objective
Đảm bảo tin đăng có hình ảnh thực tế chất lượng cao và vị trí bản đồ chính xác tuyệt đối giúp khách thuê dễ dàng định vị máy trên bản đồ.

### Scope of Work
- [ ] Xây dựng component `src/components/device/ImageUploadGrid.tsx` cho phép xem trước và xóa ảnh.
- [ ] Nút bấm "Lấy vị trí hiện tại" gọi `expo-location` lấy toạ độ GPS chính xác.
- [ ] Định dạng trường toạ độ theo chuẩn MongoDB GeoJSON: `{ type: "Point", coordinates: [longitude, latitude] }`.

### Acceptance Criteria
- [ ] Chủ máy chụp/chọn ảnh từ điện thoại thành công, ảnh hiển thị xem trước sắc nét trong khung lưới.
- [ ] Tọa độ GPS được tự động cập nhật chính xác và lưu vào MongoDB với chỉ mục `2dsphere`.

---

## ISSUE #27: [Feature] Quản lý Thiết bị của tôi (My Devices) & Status Toggle
Assignee: Kiên  
Labels: `type::feature` · `priority::P0` · `sprint::1`

### Quản lý Thiết bị của tôi Description
Xây dựng màn hình Quản lý Thiết bị của tôi (`MyDevicesScreen.tsx`) hiển thị danh sách toàn bộ các máy do chính chủ tài khoản sở hữu; tích hợp công tắc chuyển trạng thái nhanh (Switch toggle) giữa Sẵn sàng (`available`), Bảo trì (`maintenance`) và Tạm ẩn (`hidden`).

### Objective
Giúp chủ máy toàn quyền quản lý kho thiết bị của mình, chủ động tạm ẩn bài đăng khi máy đang hỏng hoặc khi chủ máy cần tự sử dụng.

### Scope of Work
- [ ] Xây dựng màn hình `src/screens/device/MyDevicesScreen.tsx`.
- [ ] Xây dựng component `src/components/device/DeviceStatusToggle.tsx` với hiệu ứng hoạt họa switch.
- [ ] Xây dựng API Backend `GET /api/devices/my-devices` và `PATCH /api/devices/:id/status`.

### Acceptance Criteria
- [ ] Danh sách hiển thị đúng và đủ toàn bộ các máy do tài khoản đang đăng nhập sở hữu.
- [ ] Gạt công tắc chuyển sang `maintenance`, máy đó lập tức biến mất khỏi Trang chủ và Bản đồ của người khác.

---

## ISSUE #28: [Feature] Lịch bận cá nhân chủ máy (Availability Calendar)
Assignee: Kiên  
Labels: `type::feature` · `priority::P1` · `sprint::2`

### Lịch bận cá nhân chủ máy Description
Xây dựng Modal Lịch bận cá nhân (`OwnerAvailabilityModal.tsx`): cho phép chủ máy chủ động chạm chọn các ngày trên lịch mà mình cần dùng máy để chặn (Block dates); hệ thống sẽ khóa các ngày này trên màn hình đặt thuê của khách.

### Objective
Ngăn chặn xung đột lịch trình cá nhân của chủ sở hữu thiết bị, đảm bảo chủ máy luôn có máy dùng khi cần thiết.

### Scope of Work
- [ ] Xây dựng Modal chọn ngày bận trên lịch tương tác.
- [ ] Lưu mảng các ngày bận vào trường `blockedDates: [Date]` trong Collection `devices`.
- [ ] Xây dựng API Backend `PATCH /api/devices/:id/blocked-dates`.

### Acceptance Criteria
- [ ] Chủ máy chọn chặn ngày 15/10 - 18/10 thành công.
- [ ] Phía người thuê khi đặt máy sẽ thấy các ngày 15/10 - 18/10 bị làm mờ xám và không thể chọn được.

---

## ISSUE #29: [Feature] Báo cáo doanh thu & Thống kê tỷ lệ lấp đầy (Utilization)
Assignee: Kiên  
Labels: `type::feature` · `priority::P1` · `sprint::3`

### Báo cáo doanh thu & Thống kê kho Description
Xây dựng màn hình Thống kê tài chính cho chủ máy (`OwnerAnalyticsScreen.tsx`): hiển thị tổng doanh thu tích lũy, số lượt máy đã cho thuê, biểu đồ cột doanh thu theo tuần/tháng và tỷ lệ lấp đầy máy (Utilization rate %).

### Objective
Cung cấp cái nhìn trực quan về hiệu quả kinh doanh của từng thiết bị công nghệ, giúp chủ máy tối ưu hóa danh mục đầu tư thiết bị.

### Scope of Work
- [ ] Xây dựng màn hình `src/screens/device/OwnerAnalyticsScreen.tsx`.
- [ ] Tích hợp thư viện biểu đồ di động (`react-native-chart-kit` hoặc SVG charts).
- [ ] Xây dựng API Backend `GET /api/devices/owner/analytics` tổng hợp doanh thu từ các đơn `completed`.

### Acceptance Criteria
- [ ] Biểu đồ doanh thu render đẹp mắt, hiển thị chính xác các khoản thu từ tiền thuê máy.
- [ ] Thống kê tỷ lệ máy được thuê tính toán đúng: `(Tổng số ngày được thuê / 30 ngày) * 100%`.

---

## ISSUE #30: [Feature] Gemini AI: Review Summarizer thẻ Pros, Cons, BestFor
Assignee: Kiên  
Labels: `type::feature` · `priority::P0` · `sprint::2`

### Gemini AI Review Summarizer Description
Tích hợp Google Gemini API (`gemini-1.5-flash`) phía server: nhận tên và thông số kỹ thuật của thiết bị, thực thi prompt kỹ thuật yêu cầu AI tóm tắt thành 3 khối nội dung JSON: Thẻ Xanh Ưu điểm (Pros), Thẻ Đỏ Nhược điểm (Cons), Lời khuyên mục đích thuê (Best For); hiển thị modal đẹp mắt trên ứng dụng.

### Objective
Tạo điểm nhấn công nghệ AI hiện đại, giúp khách thuê nhanh chóng nắm bắt ưu nhược điểm của máy trong 3 giây mà không cần đọc hàng chục bài viết dài.

### Scope of Work
- [ ] Cấu hình SDK `@google/generative-ai` trong file `server/src/config/gemini.js`.
- [ ] Xây dựng API Backend `POST /api/ai/summarize-review`.
- [ ] Xây dựng Modal giao diện `src/components/ai/AiReviewModal.tsx` và thẻ `ProsConsCard.tsx`.
- [ ] Thêm nút "AI Review" nổi bật trên màn hình Chi tiết thiết bị.

### Acceptance Criteria
- [ ] Nhấn nút "AI Review" phản hồi kết quả phân tích trong vòng dưới 5 giây.
- [ ] Nội dung bóc tách chuẩn xác 3 phần riêng biệt: Ưu điểm, Nhược điểm và Lời khuyên thuê.

---

## ISSUE #31: [Feature] Gemini AI: Device Comparator so sánh 2 thiết bị
Assignee: Kiên  
Labels: `type::feature` · `priority::P1` · `sprint::2`

### Gemini AI Device Comparator Description
Xây dựng màn hình So sánh đối đầu 2 thiết bị (`AiCompareScreen.tsx`): cho phép chọn 2 máy công nghệ bất kỳ trong kho dữ liệu; gửi thông tin đến Gemini AI để lập bảng phân tích so sánh chi tiết về sức mạnh, cảm biến, thời lượng pin, trọng lượng và hiệu năng trên giá thuê.

### Objective
Hỗ trợ người thuê giải quyết bài toán đắn đo lựa chọn giữa hai thiết bị cùng phân khúc (ví dụ: Sony A7 IV vs Canon R6 II, hoặc MacBook M3 vs Dell XPS).

### Scope of Work
- [ ] Xây dựng màn hình `src/screens/ai/AiCompareScreen.tsx`.
- [ ] Xây dựng API Backend `POST /api/ai/compare` nhận vào ID của 2 thiết bị.
- [ ] Hiển thị bảng đối chiếu thông số và kết luận khuyến nghị của AI dạng thẻ trực quan.

### Acceptance Criteria
- [ ] Chọn 2 thiết bị và nhấn "So sánh bằng AI", bảng phân tích đối đầu hiển thị rõ ràng điểm hơn kém của từng máy.
- [ ] Đưa ra kết luận khách quan về sản phẩm nào phù hợp hơn cho từng nhu cầu cụ thể.

---

## ISSUE #32: [Feature] Gemini AI: Rental Consultant & AI Listing Assistant
Assignee: Kiên  
Labels: `type::feature` · `priority::P2` · `sprint::3`

### Gemini AI Rental Consultant & Listing Assistant Description
Xây dựng 2 tính năng AI thông minh mở rộng:
1. **AI Rental Consultant**: Khách nhập câu hỏi nhu cầu tự nhiên ➔ AI quét CSDL và gợi ý 2 máy tối ưu kèm link bấm thuê.
2. **AI Listing Assistant**: Chủ máy nhập tên model ➔ AI tự động viết đoạn mô tả hấp dẫn và tự điền bảng thông số kỹ thuật chuẩn.

### Objective
Mang lại trải nghiệm trợ lý ảo thông minh toàn diện, gia tăng tỷ lệ chuyển đổi đơn thuê và hỗ trợ chủ máy đăng tin nhanh chóng.

### Scope of Work
- [ ] Xây dựng component Chat tư vấn `src/components/ai/AiChatConsultant.tsx`.
- [ ] Thêm nút "AI Viết mô tả" trên màn hình Đăng tin cho thuê.
- [ ] Xây dựng API Backend `POST /api/ai/consultant` và `POST /api/ai/generate-description`.

### Acceptance Criteria
- [ ] Nhập "Tôi muốn quay vlog du lịch biển dưới 400k" ➔ AI gợi ý đúng các dòng Action Cam/Gimbal có trong kho.
- [ ] Nhấn "AI Viết mô tả" tự sinh đoạn văn giới thiệu máy mượt mà và chuyên nghiệp.

---

## ⚙️ PHẦN 5: ISSUES CỦA THÀNH VIÊN 5 - NHẬT (LEAD)
> **Vai trò**: Team Leader & Operations/Backend Lead  
> **Luồng phụ trách**: Vận hành Đơn, Đánh giá 2 chiều, Chat thời gian thực & Admin Hub

## ISSUE #33: [Feature] Xử lý đơn phía Chủ máy (Owner Approval & Rejection)
Assignee: Nhật (Lead)  
Labels: `type::feature` · `priority::P0` · `sprint::2`

### Xử lý đơn phía Chủ máy Description
Xây dựng màn hình Quản lý đơn dành riêng cho Chủ máy (`BookingManageScreen.tsx`): xem danh sách khách gửi yêu cầu thuê máy; xem hồ sơ tín nhiệm của người thuê; cung cấp 2 nút bấm thao tác: **Duyệt đơn (`approved`)** hoặc **Từ chối (`rejected`)** kèm lý do từ chối.

### Objective
Trao quyền kiểm soát tuyệt đối cho chủ sở hữu thiết bị trong việc chọn lựa khách thuê đáng tin cậy để giao tài sản.

### Scope of Work
- [ ] Xây dựng màn hình `src/screens/booking/BookingManageScreen.tsx` phân tab Chờ duyệt / Đang cho thuê.
- [ ] Xây dựng component `src/components/booking/OwnerApprovalCard.tsx`.
- [ ] Xây dựng API Backend `PATCH /api/bookings/:id/status` cập nhật trạng thái đơn sang `approved` hoặc `rejected`.
- [ ] Tự động kích hoạt tạo thông báo gửi về cho người thuê khi có kết quả duyệt.

### Acceptance Criteria
- [ ] Chủ máy bấm Duyệt đơn, trạng thái đơn lập tức chuyển sang `approved` và thông báo tự động gửi đến máy khách thuê.
- [ ] Bấm Từ chối yêu cầu nhập lý do, đơn chuyển sang `cancelled` và mở lại lịch trống cho thiết bị.

---

## ISSUE #34: [Feature] Quét QR kích hoạt active & Ảnh đối soát trả (afterRental)
Assignee: Nhật (Lead)  
Labels: `type::feature` · `priority::P0` · `sprint::2`

### Quét QR kích hoạt & Ảnh đối soát trả Description
Xây dựng luồng bàn giao và nhận lại máy: Chủ máy mở Camera quét mã QR của khách để kích hoạt đơn sang `active` (bắt đầu tính giờ thuê); khi khách trả máy, chủ máy chụp ảnh hiện trạng thiết bị sau khi thuê (`handoverPhotos.afterRental`) và bấm Hoàn tất đơn (`completed`).

### Objective
Khép kín quy trình giao nhận thực tế ngoài đời, lưu trữ chứng cứ hình ảnh đối chiếu trước và sau khi thuê để bảo vệ quyền lợi tài sản cho chủ máy.

### Scope of Work
- [ ] Tích hợp camera quét mã QR (`expo-camera` hoặc `expo-barcode-scanner`).
- [ ] Xây dựng Modal chụp ảnh hiện trạng sau thuê (`handoverPhotos.afterRental`).
- [ ] Xây dựng API Backend `PATCH /api/bookings/:id/complete` chuyển trạng thái sang `completed` và đổi trạng thái máy về `available`.

### Acceptance Criteria
- [ ] Quét mã QR thành công kích hoạt đơn sang trạng thái `active` ngay lập tức.
- [ ] Hệ thống bắt buộc phải chụp ảnh sau thuê mới cho phép bấm nút "Hoàn tất đơn & Trả cọc".

---

## ISSUE #35: [Feature] Khiếu nại hư hỏng & Quy trình trừ cọc (Damage Dispute)
Assignee: Nhật (Lead)  
Labels: `type::feature` · `priority::P1` · `sprint::3`

### Khiếu nại hư hỏng & Trừ cọc Description
Xây dựng chức năng Báo cáo sự cố khi nhận lại máy: nếu máy bị nứt vỡ, trầy xước nặng hoặc thiếu phụ kiện, chủ máy tạo phiếu khiếu nại đính kèm ảnh bằng chứng trước/sau và mô tả thiệt hại để yêu cầu giữ cọc đền bù; chuyển hồ sơ lên Admin phân xử.

### Objective
Bảo vệ tài sản chủ máy, cung cấp cơ chế xử lý tranh chấp hư hỏng minh bạch và có bằng chứng xác thực trên hệ thống.

### Scope of Work
- [ ] Xây dựng Modal `src/components/booking/DamageDisputeModal.tsx`: nhập mô tả hư hỏng và tải ảnh bằng chứng.
- [ ] Xây dựng API Backend `POST /api/bookings/:id/dispute` lưu thông tin vào trường `dispute` trong Collection `bookings`.
- [ ] Khóa tạm thời khoản tiền cọc trong ví ký quỹ không hoàn trả tự động cho đến khi có phán quyết.

### Acceptance Criteria
- [ ] Gửi khiếu nại thành công, đơn hàng chuyển trạng thái sang "Đang giải quyết tranh chấp".
- [ ] Tiền cọc được bảo lưu an toàn trong hệ thống và thông báo gửi tức thì tới Admin để đối soát.

---

## ISSUE #36: [Feature] Hệ thống Đánh giá 2 chiều (Review & Rating 1-5 sao ảnh thật)
Assignee: Nhật (Lead)  
Labels: `type::feature` · `priority::P0` · `sprint::2`

### Hệ thống Đánh giá 2 chiều Description
Xây dựng phân hệ Đánh giá 2 chiều: Người thuê chấm điểm thiết bị & chủ máy (1 - 5 sao kèm ảnh chụp thật) sau khi đơn hoàn tất; Chủ máy chấm điểm ý thức giữ gìn máy của người thuê; hệ thống tự động tính toán lại điểm rating trung bình của thiết bị và người dùng.

### Objective
Xây dựng cộng đồng cho thuê văn minh, minh bạch thông qua hệ thống đánh giá thực chất có hình ảnh làm chứng.

### Scope of Work
- [ ] Xây dựng Modal đánh giá `src/components/review/ReviewModal.tsx`: chọn sao, viết nhận xét, tải ảnh chụp thực tế.
- [ ] Xây dựng API Backend `POST /api/reviews` có ràng buộc unique index trên cặp `booking + reviewer` (chống spam review).
- [ ] Viết hàm tính toán tự động cập nhật `ratingAverage` và `numReviews` của thiết bị và người dùng.
- [ ] Hiển thị danh sách đánh giá công khai ở cuối màn hình Chi tiết thiết bị.

### Acceptance Criteria
- [ ] Gửi đánh giá thành công, điểm số sao trung bình của thiết bị lập tức được tính toán lại chính xác.
- [ ] Hệ thống chặn thành công việc tạo đánh giá lần thứ hai trên cùng một đơn thuê.

---

## ISSUE #37: [Feature] Nhắn tin trực tiếp 1-1 In-App Chat Renter ⇋ Owner
Assignee: Nhật (Lead)  
Labels: `type::feature` · `priority::P1` · `sprint::3`

### Nhắn tin trực tiếp 1-1 Description
Xây dựng màn hình Chat trực tiếp (`ChatDetailScreen.tsx`) gắn liền theo từng đơn thuê: hỗ trợ nhắn tin văn bản, chia sẻ vị trí hiện tại, gửi ảnh chụp thiết bị và thanh gợi ý tin nhắn mẫu nhanh (Quick message templates: "Chào bạn, pin đã sạc đầy chưa?", "Mình đã đến điểm hẹn...").

### Objective
Tạo kênh trao đổi thông tin nhanh chóng, thuận tiện và an toàn giữa hai bên mà không cần chia sẻ số điện thoại cá nhân ra ngoài.

### Scope of Work
- [ ] Xây dựng màn hình `src/screens/chat/ChatDetailScreen.tsx`.
- [ ] Xây dựng thanh mẫu tin nhắn nhanh `src/components/chat/QuickMessageBar.tsx`.
- [ ] Xây dựng API Backend: `POST /api/chats/:bookingId/messages` và `GET /api/chats/:bookingId`.
- [ ] Hiển thị trạng thái tin nhắn: Đã gửi, Đã nhận, Đã xem.

### Acceptance Criteria
- [ ] Tin nhắn gửi và hiển thị tức thì trên khung chat với bong bóng chat phân biệt 2 màu.
- [ ] Chạm vào tin nhắn mẫu nhanh sẽ tự động gửi nội dung mà không cần gõ bàn phím.

---

## ISSUE #38: [Feature] Trung tâm Thông báo (Notification Hub) & Nhắc hạn trả
Assignee: Nhật (Lead)  
Labels: `type::feature` · `priority::P0` · `sprint::2`

### Trung tâm Thông báo Description
Xây dựng Trung tâm thông báo trong ứng dụng (`NotificationScreen.tsx`): tự động sinh thông báo theo thời gian thực khi đơn hàng đổi trạng thái (chủ máy duyệt, từ chối, bàn giao, nhắc nhở hạn trả máy trước 6h và 2h); hỗ trợ chấm đỏ báo tin chưa đọc và nút đánh dấu đã đọc.

### Objective
Đảm bảo cả Renter và Owner luôn cập nhật tức thì mọi diễn biến của đơn thuê, phòng ngừa việc quên trả máy gây phát sinh phí phạt trễ hạn.

### Scope of Work
- [ ] Xây dựng màn hình `src/screens/notification/NotificationScreen.tsx`.
- [ ] Xây dựng component `NotificationBadge.tsx` hiển thị số thông báo chưa đọc trên Header.
- [ ] Viết hàm helper backend `createNotification()` kích hoạt tự động trong các Controller đặt đơn và duyệt đơn.
- [ ] Xây dựng API Backend `GET /api/notifications` và `PATCH /api/notifications/:id/read`.

### Acceptance Criteria
- [ ] Khi chủ máy bấm Duyệt đơn, tài khoản người thuê nhận được thông báo mới ngay lập tức.
- [ ] Chạm vào thông báo sẽ đánh dấu là đã đọc (mất chấm đỏ) và chuyển nhanh tới chi tiết đơn thuê tương ứng.

---

## ISSUE #39: [Feature] Ví ký quỹ nội bộ Mock (Escrow Wallet) & Hoàn cọc
Assignee: Nhật (Lead)  
Labels: `type::feature` · `priority::P2` · `sprint::3`

### Ví ký quỹ nội bộ Mock Description
Xây dựng màn hình Ví cá nhân (`WalletScreen.tsx`): hiển thị số dư khả dụng và số dư tiền cọc đang bị đóng băng (Escrow Deposit); dòng tiền cọc được giữ an toàn trong ví trong suốt thời gian thuê và tự động hoàn trả ngay khi đơn hoàn tất; xem lịch sử biến động số dư.

### Objective
Mô phỏng mô hình tài chính ký quỹ an toàn, giải quyết bài toán niềm tin giữ cọc và hoàn cọc giữa hai người dùng xa lạ.

### Scope of Work
- [ ] Xây dựng màn hình `src/screens/profile/WalletScreen.tsx`.
- [ ] Hiển thị 2 tài khoản: Số dư khả dụng và Số dư cọc tạm giữ.
- [ ] Xây dựng component danh sách lịch sử biến động số dư (Nạp tiền, Đóng cọc, Hoàn cọc, Nhận tiền thuê).
- [ ] Xây dựng API Backend: `GET /api/wallet/me` và `POST /api/wallet/topup`.

### Acceptance Criteria
- [ ] Khi tạo đơn thuê, số tiền cọc được chuyển sang trạng thái "Đang tạm giữ" trong ví.
- [ ] Đơn hàng hoàn tất (`completed`), số tiền cọc tự động mở khóa về lại số dư khả dụng ngay lập tức.

---

## ISSUE #40: [Feature] Trung tâm Quản trị Admin (Admin Hub & Platform Governance)
Assignee: Nhật (Lead)  
Labels: `type::feature` · `priority::P1` · `sprint::3`

### Trung tâm Quản trị Admin Description
Xây dựng phân hệ Quản trị viên (Admin Hub): Dashboard phân tích số liệu toàn sàn (tổng user, tổng máy, doanh thu sàn, biểu đồ danh mục); kiểm duyệt và gỡ bỏ tin đăng vi phạm; đối soát ảnh trước/sau bàn giao để giải quyết tranh chấp cọc; phê duyệt hồ sơ eKYC cấp Tích xanh uy tín.

### Objective
Trang bị công cụ quản lý vận hành toàn diện cho ban quản trị nền tảng TechShare, đảm bảo kiểm soát chất lượng dịch vụ và xử lý công bằng các tranh chấp phát sinh.

### Scope of Work
- [ ] Xây dựng middleware kiểm tra quyền Admin: `adminMiddleware.js` (`req.user.role === 'admin'`).
- [ ] Xây dựng màn hình `src/screens/admin/AdminDashboardScreen.tsx`.
- [ ] Chức năng Gỡ bài đăng vi phạm: gọi API `DELETE /api/admin/devices/:id`.
- [ ] Chức năng Duyệt eKYC cấp tích xanh: gọi API `PATCH /api/admin/verify-user/:id`.
- [ ] Chức năng Phân xử tranh chấp cọc: màn hình đối chiếu song song 2 ảnh `beforeRental` và `afterRental` để đưa ra quyết định xử lý tiền cọc.

### Acceptance Criteria
- [ ] Chỉ tài khoản có quyền `admin` mới truy cập được vào các endpoint và màn hình quản trị này.
- [ ] Admin duyệt eKYC thành công, tài khoản chủ máy lập tức xuất hiện icon Tích xanh trên toàn hệ thống.
- [ ] Admin ra phán quyết tranh chấp cọc, hệ thống tự động giải phóng hoặc trích tiền cọc đền bù theo đúng phán quyết.
